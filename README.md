# Demo project using typescript, react-native-ui-lib to buid UI and using createSlide to config store
## How to run project?
- cd demo/
- yarn install
- cd ios/ && pod install
- react-native run-android (for android) 
- react-native run-ios (for ios)

